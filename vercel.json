{
    "buildCommand": "cd backend && npm install --include=dev --legacy-peer-deps && npm run build",
    "outputDirectory": "backend/dist",
    "env": {
          "NPM_FLAGS": "--legacy-peer-deps"
    }
}
